deploy:
  stage: 'deploy(部署)'
  tags:
    - online
  only:
    refs:
      - deploy
      - tags
  needs:
    - job: build_image
  before_script:
    - source .gitlab/scripts/deploy.sh
  cache: {}
  script:
    - image_deploy "$DST_HOSTS"
